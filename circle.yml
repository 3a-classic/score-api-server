test:
  override:
    - ./dummy_test_script.sh
deployment:
  production:
    branch: master
    commands:
      - ssh circle@api.3a-classic.com -t "cd ~/3a-classic/server/score-api-server && bash ~/3a-classic/server/score-api-server/docker_run.sh"
  staging:
    branch: stage
    commands:
      - ssh circle@api.3a-classic.com -t "cd ~/3a-classic/server/score-api-server && bash ~/3a-classic/server/score-api-server/docker_run.sh"
